1. The problems primarily seemed to all be related to reloading the pages — using href rather than other methods of loading URL variables and applying them to functions.
2. There seemed to be an unneeded reload that was causing the issues and confusion; first, onChange or onSubmit functions would take a change and save them. They would then try to navigate to a new url (unneeded reload) to try and load the data. From there, there seemed to be (but I couldn’t figure out where exactly) there was a problem in dependency files with mismatched data/types that would cause a crash on loading the new page. This would then fail back to the home page.
3. Now that the issue seems to have been identified (and is reproducible in production) my next steps would to try and reproduce in a development environment, and presuming that it still is not able to be determined there, it would be to add more robust error handling throughout. I’m not super familiar with JS but I do know that it has a tendency to fail very silently and just keep working, so adding more early checks could help determine the problem without terminal logs.